/**
 * Constants for local storage keys used throughout the application.
 */

/** Key used to store the selected resume template in the builder. */
export const STORAGE_KEY_SELECTED_TEMPLATE = 'selectedTemplate';

/** Key used for the resume store persistence. */
export const STORAGE_KEY_RESUME_STORE = 'dynamicResumeData';
